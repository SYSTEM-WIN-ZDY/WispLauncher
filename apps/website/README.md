# Wisp Website

The official website for WispLauncher.

## Development

From the repository root:

```sh
pnpm install --frozen-lockfile
pnpm website:dev
```

Create the static production output with `pnpm website:build`.
